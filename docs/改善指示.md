# 改善指示

high:すぐ修正
mid:ある程度優先度高めで
low:割とすぐできそう


## UI改善

* ポップアップの各種操作ボタンが無理やり1列に押し込まれているせいで見栄えが悪い high
* 全体設定で削除しかできない。文字をいじったりできるようにしてほしい mid
* markdownで、h1~h6のサイズが普通のテキストと同じ、linkがクリックできない など high
* 一度縮小してから再拡大すると、「メモ」と最小化アイコンの表示がおかしくなる mid =>凍結

## バグ

* トリガーを「クリック」にした瞬間モーダルが閉じる（情報は保存されていない) high ただし難易度高そう

## 0120 追加指摘

* 依然として、付箋部分をダブルクリックで編集にいけない（テキスト部分のちょっと上をダブルクリックで編集できる？）
* h1-h6がデカすぎる。今のh3がh1になるくらいでいい。
* 付箋のサイズにかかわらず、常にスクロールバーが表示される(どれだけサイズをデカくしてもほんの少しだけスクロールできる）。また、編集画面ではスクロールバーが重複して表示される
* 一番上のh1をタイトルとして設定する。

## 未解決の問題 (Known Issues)

* 一度縮小してから再拡大すると、メモのタイトルバーがpadding分だけ外にずれる（アニメーション終了後のtransformリセットでは解決せず）
* スクロールバーが常に表示される問題（要調査）


# 改善指示0128


# ホバー・クリックの対象表示

設定画面で、どこホバーしたら出てくるのかを見れるようなSomethingが必要かも。

あとは、ホバー・クリックのセレクタピッカーを試す事ができるようにとか？候補出てきたときに実際にホバーしてみてどうなるかを見るとか

-> 未着
優先度：中

# 浮いてる要素の警告

ページにマッチしてるがアクティブ化対象のセレクタが見つからないとき、浮いてるメモをどこかにまとめる。
ポップアップか、画面の中に最小化みたいに表示してリスト出すか。
もちろんリアルタイムで更新。

-> 未着
優先度：低

# そのページに存在してるメモの数を拡張機能アイコンのドットで表示

-> 完了（0129対応）
  - タブ切り替え、ページ遷移、SPA遷移、メモ変更時にバッジを更新
  - background.ts に updateBadgeForTab() を追加

# ワイルドカードの流用

現在のページにあるメモのワイルドカードを候補として出す。
また、設定で、「新規メモを作ったとき、既存のメモのワイルドカードをデフォルトに設定する」みたいなフラグを用意する

-> 部分的に完了（0129対応）
  - SettingsModal に「既存のパターンを再利用」ボタンを追加
  - 同じページにマッチする他メモのパターンを候補として表示
  - 「デフォルト設定」フラグは未実装

# （ほんとかは分からん） ホバー・クリック対象を子要素にも広げる

クリックしたのに出てこねえぞ……？が起こりがち。

-> 解決（0.9.0）
  - useActivation.ts に isEventFromElementOrChild() ヘルパー追加
  - 子要素のクリックでも親要素のアクティブ化が発火するように


# 起きてる問題

一部ページにおいてリロード後にホバー・クリックなどが効かない（SPAだから？いや関係ない気はするが…）
まあ全体的にアクティブ化は完成度が低い。

なお。要素のセレクタが変わっているわけではない。
また、再設定後はリロードするまで使用可能である。

これと関連するかは微妙だが、executive inline script violates the following content security policy directive script-src self wasm-unsafe-eval inline-speculation-rules http://localhost/* http://127.0.0.1:* chrome-extension://省略 
というエラーが出ていた。


-> 解決。依然として完成度は低いが。

# アクティブ化を設定すると、ピン止め・解除で座標がずれる
-> 解決

# アクティブ化を設定すると、周辺モードでは動かせない領域が生まれ、固定モードではうまく動かせないうえに非表示にもなる
-> 一部解決。

# 起きてる問題と要望0129

# 右クリックメニューで、右クリックした要素をアクティブ化のトリガーとしてメモを作成

-> 完了（0129対応）
  - コンテキストメニューに「この要素でメモを表示」を追加
  - 右クリックした要素のセレクタを自動取得
  - アクティブ化設定が有効なメモを作成し、設定モーダルを自動で開く

# 起きてる問題

・とくにアクティブ化を有効にしているとき、メモのサイズ、フォントサイズ、タイトルやマッチするページなど、設定値の更新がうまく行えないことがある。決定押しても更新されてない。

-> 解決
  - 設定変更（title, urlPatterns, activation, 色, fontSize）は即時保存に変更
  - position変更のみデバウンス（ドラッグ中の頻繁な更新のため）
  - MemoContainer.tsx の handleMemoUpdate を修正